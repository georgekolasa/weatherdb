module.exports = function validateQuery(req, res, next) {
  console.log('implement me please');

  /**
   * Example:
   *
   * if (bad query) {
   *  res.status(400).send("bad query man im sorry")
   * } else {
   *  next()
   * }
   */

  next();
};
